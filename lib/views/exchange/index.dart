import 'package:flutter/material.dart';

class ExchangeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('ExchangeView'),
        ),
      ),
    );
  }
}
